# Desarrollo: copiar a .env.development.local si querés sobreescribir sin tocar el repo
VITE_BASE_PATH=/
VITE_API_BASE_URL=http://localhost:8080

# Producción (referencia; el build usa .env.production)
# VITE_BASE_PATH=/logistica
# VITE_API_BASE_URL=https://kyz.com.ar/logistica/backend
#
# Generar dist/ siempre con Docker (raíz del repo):
#   docker compose --profile build run --rm frontend_build
